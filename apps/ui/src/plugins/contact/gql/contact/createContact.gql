mutation createContact($input: input!) @api(name: ai) {
  createContact(input: $input)
    @rest(type: "Contact", path: "/contact", method: "POST", bodyKey: "input", endpoint: "ai") {
    id
    name
    description
    group_id
    email
    phone
  }
}
