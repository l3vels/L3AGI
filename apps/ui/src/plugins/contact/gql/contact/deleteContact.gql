mutation deleteContact($id: id!) @api(name: ai) {
  deleteContact(id: $id)
    @rest(
      type: "Group"
      path: "/contact/{args.id}"
      method: "DELETE"
      bodyKey: "input"
      endpoint: "ai"
    ) {
    id
  }
}
